# Redis configuration for development
# Force standalone master mode (prevent accidental slave replication)
replicaof no one
slave-read-only no

# Persistence
appendonly yes
appendfsync everysec

# Network
bind 0.0.0.0
port 6379

# Security (development only)
protected-mode no
