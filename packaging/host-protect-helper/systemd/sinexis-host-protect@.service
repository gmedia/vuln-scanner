[Unit]
Description=Sinexis Host Protect on-box poll (%i)
Requires=wazuh-agent.service
After=wazuh-agent.service
# StartLimit* is a [Unit] key. In [Service] it is ignored and the default 5/10s remains.
StartLimitIntervalSec=0

[Service]
Type=oneshot
Nice=15
TimeoutStartSec=180
SuccessExitStatus=6
EnvironmentFile=-/etc/sinexis/host-protect.env
ExecStart=/usr/bin/python3 /usr/lib/sinexis/host-protect/sinexis_host_scan.py poll --agent-id %i
User=root
NoNewPrivileges=true
ProtectSystem=strict
# Quarantine dest + allowlisted web roots (S10 jail). Without these, os.rename fails closed.
# Leading '-' = ignore missing path (systemd 226/NAMESPACE if /srv/www absent).
ReadWritePaths=/var/lib/sinexis /var/www -/srv/www /home
# Lab/customer WAF ingest: helper tails ModSecurity JSON audit (no request bodies).
ReadOnlyPaths=-/var/log/modsec_audit.log
PrivateTmp=true
PrivateDevices=true
ProtectKernelTunables=true
ProtectControlGroups=true
RestrictSUIDSGID=true
LockPersonality=true
RestrictRealtime=true
RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6
SystemCallArchitectures=native

[Install]
WantedBy=multi-user.target
