# handoff.md (stub + pointer)

> **Not the product backlog.** Live roadmap and feature priority live only in
> **[`docs/AGENT_EXECUTION_GUIDE.md`](docs/AGENT_EXECUTION_GUIDE.md)**.

## After session reset

1. Read **`docs/AGENT_EXECUTION_GUIDE.md`** (§0 boot, then **§1.3.1** development queue — **never** GTM/AM/sales).
2. Read **`AGENTS.md`** for git/PR rules.
3. Do **not** implement until the user says so (`implement` / `buat` / `kerjakan` / `fix` / …) or points at an approved `docs/specs/*` section. **Never** answer “lanjut” with AM/sales/marketing checklists.
4. **Hosts:** the machine used for OpenCode / day-to-day coding is **coding only**. **Production** is the host that serves **`sinexis.app`** (public DNS; legacy `vs.appmedia.id` may still exist). Do **not** treat coding-host Docker or local health as production attach proof. Prefer full-stack Docker on the **edge** host; on the coding host keep Docker **off or minimal** (RAM for the agent).

## Session snapshot (2026-09-22 — agent = development only)

| Item | State |
|------|--------|
| **`main` tip** | Re-`git pull`. Expect commit after **#820** (Inbox S2 bounce/SES merge) or newer. Do **not** SSH Alembic after a green `main` deploy. Alembic head: `add_inbox_s2_bounce` (was `host_sku_invoicable` before #820). |
| **This session** | Chat user = **product**. Owner restated **2026-09-21:** agent = **development only** — never pitch sales / AM / marketing / GTM / first meeting / 10 SIDs. Named queue **§1.3.1:** **Inbox S2 shipped (#820)** → **inotify** → **WAF 1147+**. **Print S2 shipped (#817 merged).** **H9 Host invoicable shipped.** Still wait for named + `buat`. **Invoice-ok = Sinexis in-app** (I1/D5 shipped) — **not** GMD `service_id`. **Never** put real account numbers / holder names / emails / SIDs in git. |
| **Inbox S2 shipped (#820 merged 2026-09-22)** | Statuses `bounced`/`complained` + `provider`/`provider_message_id`/`bounce_*` cols + `email_bounce_events` + `email_suppressions` (migration `add_inbox_s2_bounce`). SES send-path (boto3 lazy, SMTP fallback) + Message-ID + pre-send suppression gate. Public `POST /api/webhooks/ses` (SNS verify, idempotent). SPA Bounced/Complained badges + filter + id/en keys. **Ops residual:** provision SES config-set + SNS topic → set `SNS_TOPIC_ARN` (+ `SES_ENABLED`/`SES_WEBHOOK_TOKEN`) in prod env → simulator smoke. |
| **Demo-ok (`tc5`)** | **Proven 2026-09-20.** **B1:** wipe-first guide §4.1; enroll wazuh-agent **011** (stdin `manage_agents -i /dev/stdin` unreliable — interactive key import + confirm `y`); helper from `sinexis.app` (`wget`, not `curl\|bash`); `helperPolled` fresh. **B2:** `./scripts/host-protect-lab-smoke.sh --keep-site` — YARA `hit_count=1`, SPA quarantine → restore. **B3:** ModSec loaded on **lab** vhost listen `:8088` `server_name host-waf-lab.local`; live **403** id **1005** (`POST /wp-login.php` + query-string ARGS) and **1006** (`GET /eval(`, URI `base64_decode(`); `GET /wp-admin/` **200**. `--apply-vhost` copies snippet to **`/tmp/sinexis-host-waf-lab.conf`** (does **not** rewrite nginx include; live include already `/etc/nginx/snippets/sinexis-host-waf-lab.conf`). Never paste onto `sinexis.app` edge. **B4:** spoken exclusions reported — no PD, WebShield, MDS auto-clean DB, KernelCare, Caddy-as-supported, shared cPanel “ganti”. |
| **B3 residual (not a fail)** | POST **body-only** `/wp-login.php` → **404**: snippet `SecRequestBodyAccess Off`; fixture has **no** `wp-login.php` file. Query-string 403 is the honest 1005 proof. **Do not** rebuild P13/DL2 unless named. |
| **Lab leftovers (names only)** | Sites kept: `lab-host-protect-fixture`, `lab-host-waf-fixture`. Never wipe `sx-erpstg` / Manager **000** / agent **010**. Lab env file **names** only (`/tmp/opencode/guard-lab-run.env`); token files mode 600 — **never print values**. |
| **Shipped this wave (docs)** | Agent-dev-only lock (this stub + Guide §1.3.1). Sentence-ok GREEN **#813**. Invoice-ok = Sinexis in-app **#811**. Demo-ok `tc5` **#810**. `handoff.md` retarget **#814**. PR **#817** (Print S2 scan PDF + mypy CI fix). |
| **Shipped this wave (code)** | Uptime overview **#807/#808**. Inbox S1c **#793**. Invite honesty **#794** then SMTP **#800**. Status hostname **#795**. Print popup + invoice “Mark sent” **#796**. Findings `?severity=&q=` **#797** + pager **#803**. Schedules last-run Print **#799**. First-scan notify **#801**. Guard D10 `disabled_at` **#802**. Status incidents **#798/#804**. Alembic merge **#805**. |
| **Print** | Browser Print for scan HTML + invoice SPA. ScanDetail **Download PDF** (`export-pdf`, `format=pdf` WeasyPrint of executive HTML) — **#817 merged**. Schedules last-run stays `printFile(..., "executive")`. Invoice PDF stays I10 **out**. Spec [`scan-pdf-invoice-print-v1.md`](docs/specs/scan-pdf-invoice-print-v1.md). |
| **Inbox** | User SMTP log + `job_id` link for `scan_diff`. **S2 shipped (#820):** `bounced`/`complained` via SES+SNS webhook + suppression + SPA badges. Invite kind is **admin log only**, not Inbox. Next: **inotify** = §1.3.1 **#2**. Spec [`inbox-delivered-v1.md`](docs/specs/inbox-delivered-v1.md). |
| **Guard D10** | SPA soft-disable (`disabled_at`). **No** Wazuh DELETE. Hard delete = ops. Discover still Out. Spec [`guard-v1.md`](docs/specs/guard-v1.md). |
| **P14 E** | Hourly **already on `main`**. inotify = §1.3.1 **#2**. WAF **1147+** = **#3**. **Not** 24×7 YARA on `/`. **G/H** research only. |
| **Sentence-ok** | **GREEN 2026-09-21** (nginx dedicated / no panel). Owner **confirmed in chat** after #813 merge: short *“ganti Imunify360 dengan Sinexis di VPS/server ini”* **allowed** on **one VPS / dedicated nginx without a panel**. **Must still say:** no PHP Proactive Defense, KernelCare, WebShield, or auto-clean database. **Forbidden:** shared cPanel / CloudLinux farm; buyer who **requires** PD / KernelCare / WebShield (sit beside; sell Scan attach); quote Host Basic while demoing Protect/WAF (Protect = org already **Host Multi** before the meeting); Simulate as live block; “Imunify compatible”. **H9 Host invoicable shipped.** **Do not** wait on GMD Host `service_id`. Spec [`vps-displace-imunify-dev-plan.md`](docs/specs/vps-displace-imunify-dev-plan.md) §7. |
| **Invoice product** | Pay loop closed. Send = status flip (honest copy). Invite SMTP ≠ invoice SMTP. Spec [`sinexis-invoice-v1.md`](docs/specs/sinexis-invoice-v1.md) **I1**: Sinexis bills, independent of GMD. Host catalog `invoicable=true` (**H9 shipped**). Invoice-ok for the short line = **Sinexis in-app**, **not** GMD SID. Paid Host does **not** change Scan `org.sku`. |
| **Metering v3** | **Shipped**. Scan/Host/Uptime = SKU seats; `credit_cost = 0`. **Do not** re-open Scan credit debit. |
| **Open PRs** | Boot: `gh pr list --state open --assignee @me`. Dependabot: **do not mass-merge**. |
| **Still human (off-repo — never pitch)** | Optional leftover: GMD Host `service_id` (same as Scan D5 — **not** Invoice-ok, **not** agent next). GTM / `/admin/hpp` `hostscan` / prod attach smoke / first AM meeting on a customer nginx VPS. **Never** lead with these. |
| **Engineering default** | §1.3.1 named queue: **1 Inbox S2 shipped (#820)** → **2 inotify** → **3 WAF 1147+**. **Print S2 shipped.** **H9 shipped.** **“lanjut”** without a named slice → recommend **inotify**; still wait for `buat`. Agent = **development only**. **Do not** silent-code. **Do not** re-run wipe/enroll/B3 unless asked. **Do not** rebuild P12/P13/DL0–DL3. **Do not** pitch `service_id` / 10 SIDs / AM meeting / GTM. Still parked (not this queue): P14 **G/H**, Capacitor, invoice PDF (I10), 24×7 YARA on `/`. Invoice v1 closed. Speak **Bahasa Indonesia**. Prefix git **`GIT_MASTER=1`**. Never work on `main`. Never print tokens/IPs/customer paths/bank details. Never commit `.omo/` or screenshot PNGs. |

### Next OpenCode session

1. `GIT_MASTER=1 git checkout main && GIT_MASTER=1 git pull`. `gh pr list --state open --assignee @me`. CI green on **your** PRs → squash-merge then delete branch. **Do not poll CI.** Do **not** mass-merge Dependabot.
2. Read **`docs/AGENT_EXECUTION_GUIDE.md`** then **`AGENTS.md`**. Guide **wins** on epic order vs this stub.
3. If user says **“lanjut”** without a named slice: recommend **inotify** (Guide §1.3.1 #2 — Inbox S2 shipped #820). **Do not** implement until `buat`. Demo-ok B1–B4 is **proven.** Invoice-ok = **Sinexis in-app shipped** (I1/D5). **Print S2 shipped (#817 merged).** **Inbox S2 shipped (#820 merged).** **H9 Host invoicable shipped.** **Sentence-ok GREEN** 2026-09-21 (nginx dedicated / no panel). Short line **allowed** nginx-only. **Never** recommend GMD Host `service_id`, AM meeting, GTM, or 10 SIDs. **Do not** treat “ganti Imunify” as a code epic. **Do not** re-wipe `tc5` / re-enroll / re-apply-vhost unless the user names it. **Do not** silent-code Guard Discover / WAF 1147+ / G/H.
4. Mypy CI note: pre-commit disables `no-any-return` and `type-arg` (`--disable-error-code=...`); CI does **not**. When a function returning `bytes`/`str` has an `Any`-typed upstream call (e.g. `write_pdf()`), guard with `isinstance(x, bytes) or not x` to satisfy strict mypy — **never** use `as any`/`type: ignore`.
5. Sentence-ok (only if asked): short line **allowed** on **one nginx VPS / dedicated without a panel** ([`vps-displace-imunify-dev-plan.md`](docs/specs/vps-displace-imunify-dev-plan.md) §0). File-only demo = quote **Host Basic**. WAF/protect sentence = org is **Host Multi before** the meeting. Buyer who **requires** PD / KernelCare / WebShield is **not** a displace deal — sit beside; sell Scan attach. Shared cPanel “ganti” still **forbidden**.
6. Host WAF live lab (only if asked again): leftover `lab-host-waf-fixture` may already exist (`--keep-site`). `HOST_WAF_LAB_ALLOW_PUBLIC_PROD=1`; `HOST_WAF_LAB_VHOST_SSH=tc5`; `./scripts/host-waf-lab-smoke.sh --apply-vhost` copies to `/tmp/sinexis-host-waf-lab.conf` — **operator must include** on lab vhost (already included). `sudo nginx -t`. Standing permission **2026-08-26** is **Guard enroll/unenroll** (wipe `tc5` first, guide **§4.1**), not a blank cheque to keep applying vhosts. **Never** wipe `sx-erpstg`. Playwright ≠ enroll.
7. Guard enroll (only if asked): wipe-first §4.1. Stdin import is **unreliable**; import with `-i "$KEY"` + confirm `y`. Never delete Manager **000**. Never wipe agent **010** (`sx-erpstg`).
8. Inbox files if asked: `backend/app/api/inbox_routes.py`, `backend/app/models/email_send_log.py` (`user_id`, `job_id`), `frontend/src/pages/Inbox.tsx`, spec [`inbox-delivered-v1.md`](docs/specs/inbox-delivered-v1.md).
9. Print files if asked: `frontend/src/api/scans.ts` (`printFile`, `downloadFile(..., "pdf")`), `ScanDetail.tsx` (`export-pdf`, `export-print-executive`), `backend/app/services/scan_pdf.py`, spec [`scan-pdf-invoice-print-v1.md`](docs/specs/scan-pdf-invoice-print-v1.md). Schedules stays HTML print. Scan PDF backend + SPA in **#817**.
10. Guard D10 if asked: `POST /api/guard/agents/{id}/disable`, `guard_agents.disabled_at`. Hard delete Wazuh = ops.
11. Older snapshots (including **2026-09-21 #813 pin / empty queue**, **2026-09-21 GREEN still pinned to #811**, **2026-09-20 Demo-ok + Sentence-ok docs #809/#810**, **2026-09-17 first-scan/D10**, **2026-09-16 print + inbox**, **2026-09-15 Billing empty**, **invoice tests / metering v3**, **SPA chrome**, **#733 / tc5**): [`docs/archive/handoff-session-snapshots-2026-09.md`](docs/archive/handoff-session-snapshots-2026-09.md). Stuck-job notes: [`docs/archive/handoff-scan-pending-2026.md`](docs/archive/handoff-scan-pending-2026.md). Live Sentence-ok is **GREEN 2026-09-21** — archive rows that still say OPEN, pin SHA **#811**, or “queue is empty” are **history**.
