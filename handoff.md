# handoff.md (stub + pointer)

> **Not the product backlog.** Live roadmap and feature priority live only in
> **[`docs/AGENT_EXECUTION_GUIDE.md`](docs/AGENT_EXECUTION_GUIDE.md)**.

## After session reset

1. Read **`docs/AGENT_EXECUTION_GUIDE.md`** (§0 boot, then **§1.3.1** product-depth queue — **not** GTM).
2. Read **`AGENTS.md`** for git/PR rules.
3. Do **not** implement until the user says so (`implement` / `buat` / `kerjakan` / `fix` / …) or points at an approved `docs/specs/*` section. **Do not** answer “lanjut” with AM/sales checklists.
4. **Hosts:** the machine used for OpenCode / day-to-day coding is **coding only**. **Production** is the host that serves **`sinexis.app`** (public DNS; legacy `vs.appmedia.id` may still exist). Do **not** treat coding-host Docker or local health as production attach proof. Prefer full-stack Docker on the **edge** host; on the coding host keep Docker **off or minimal** (RAM for the agent).

## Session snapshot (2026-09-20 — Demo-ok B1–B4 proven; Sentence-ok still OPEN)

| Item | State |
|------|--------|
| **`main` tip** | Re-`git pull`. Expect **`effd4646`** (`docs: Sentence-ok closure path (still OPEN)` **#809**) or newer. Do **not** SSH Alembic after a green `main` deploy. Alembic head: `merge_invite_kind_guard_disabled`. |
| **This session** | Chat user = **product**. After #809 merge: user asked next steps then **“kerjakan sesuai urutan sampai selesai”** — Demo-ok **B1–B4 on `tc5`**. **B1–B4 proven.** **Sentence-ok stays OPEN.** Short “ganti Imunify360 dengan Sinexis” still **forbidden**. Remaining = Invoice-ok **C1** (GMD `service_id`, off-repo) + owner dates §7 E. H9 / Host `invoicable` **not** named. **Never** put real account numbers / holder names / emails / SIDs in git. |
| **Demo-ok (`tc5`)** | **Proven 2026-09-20.** **B1:** wipe-first guide §4.1; enroll wazuh-agent **011** (stdin `manage_agents -i /dev/stdin` unreliable — interactive key import + confirm `y`); helper from `sinexis.app` (`wget`, not `curl\|bash`); `helperPolled` fresh. **B2:** `./scripts/host-protect-lab-smoke.sh --keep-site` — YARA `hit_count=1`, SPA quarantine → restore. **B3:** ModSec loaded on **lab** vhost listen `:8088` `server_name host-waf-lab.local`; live **403** id **1005** (`POST /wp-login.php` + query-string ARGS) and **1006** (`GET /eval(`, URI `base64_decode(`); `GET /wp-admin/` **200**. `--apply-vhost` copies snippet to **`/tmp/sinexis-host-waf-lab.conf`** (does **not** rewrite nginx include; live include already `/etc/nginx/snippets/sinexis-host-waf-lab.conf`). Never paste onto `sinexis.app` edge. **B4:** spoken exclusions reported — no PD, WebShield, MDS auto-clean DB, KernelCare, Caddy-as-supported, shared cPanel “ganti”. |
| **B3 residual (not a fail)** | POST **body-only** `/wp-login.php` → **404**: snippet `SecRequestBodyAccess Off`; fixture has **no** `wp-login.php` file. Query-string 403 is the honest 1005 proof. **Do not** rebuild P13/DL2 unless named. |
| **Lab leftovers (names only)** | Sites kept: `lab-host-protect-fixture`, `lab-host-waf-fixture`. Never wipe `sx-erpstg` / Manager **000** / agent **010**. Lab env file **names** only (`/tmp/opencode/guard-lab-run.env`); token files mode 600 — **never print values**. |
| **Shipped this wave (code)** | Sentence-ok docs **#809**. Uptime overview **#807/#808**. Inbox S1c **#793**. Invite honesty **#794** then SMTP **#800**. Status hostname **#795**. Print popup + invoice “Mark sent” **#796**. Findings `?severity=&q=` **#797** + pager **#803**. Schedules last-run Print **#799**. First-scan notify **#801**. Guard D10 `disabled_at` **#802**. Status incidents **#798/#804**. Alembic merge **#805**. |
| **Print** | Browser Print for scan HTML + invoice SPA. Schedules last-run uses `printFile(..., "executive")`. **`format=pdf` still 400.** WeasyPrint parked S2. Spec [`scan-pdf-invoice-print-v1.md`](docs/specs/scan-pdf-invoice-print-v1.md). |
| **Inbox** | User SMTP Sent/Failed + `job_id` link for `scan_diff`. Invite kind is **admin log only**, not Inbox. Bounce/SES = S2 parked. Spec [`inbox-delivered-v1.md`](docs/specs/inbox-delivered-v1.md). |
| **Guard D10** | SPA soft-disable (`disabled_at`). **No** Wazuh DELETE. Hard delete = ops. Discover still Out. Spec [`guard-v1.md`](docs/specs/guard-v1.md). |
| **P14 E** | **Already on `main`**. Out still: 24×7 YARA on `/`, inotify unless named, WAF **1147+**, **G/H**. |
| **Sentence-ok** | **OPEN.** Demo-ok **proven**. Pack 1005+ + Simulate lab-gate **shipped**. ModSec on lab vhost **proven**. Still need **C1** three Host `service_id` (off-repo) **AND** product owner dates private checklist §7 E. **AND not OR.** Do **not** mark GREEN. Spec [`vps-displace-imunify-dev-plan.md`](docs/specs/vps-displace-imunify-dev-plan.md) §7. |
| **Invoice product** | Pay loop closed. Send = status flip (honest copy). Invite SMTP ≠ invoice SMTP. Spec [`sinexis-invoice-v1.md`](docs/specs/sinexis-invoice-v1.md). Host catalog still `invoicable=false` until named H9. Invoice-ok for the short line = **C1 `service_id`**, not flipping `invoicable`. |
| **Metering v3** | **Shipped**. Scan/Host/Uptime = SKU seats; `credit_cost = 0`. **Do not** re-open Scan credit debit. |
| **Open PRs** | Boot: `gh pr list --state open --assignee @me`. Dependabot: **do not mass-merge**. |
| **Still human (off-repo)** | **C1** three Host `service_id` (Basic/Pro/Multi, not bundled into VPS/Scan). **E** owner-dated private checklist. GTM / `/admin/hpp` `hostscan` / prod attach smoke. **Do not** lead with GTM. |
| **Engineering default** | §1.3.1 **queue is empty of new default code**. **“lanjut”** without a named slice → Demo-ok is **done**; remaining is **human C1 + E**; wait for named + `buat`. **Do not** silent-code. **Do not** re-run wipe/enroll/B3 unless asked. **Do not** rebuild P12/P13/DL0–DL3. **Do not** pitch `service_id` / 10 SIDs as next **code**. Parked unless named: print S2 (`format=pdf`/WeasyPrint), inbox S2 (bounce/SES), P14 **G/H**, WAF **1147+**, inotify, Capacitor, Host invoice H9. Invoice v1 closed. Speak **Bahasa Indonesia**. Prefix git **`GIT_MASTER=1`**. Never work on `main`. Never print tokens/IPs/customer paths/bank details. Never commit `.omo/` or screenshot PNGs. |

### Next OpenCode session

1. `GIT_MASTER=1 git checkout main && GIT_MASTER=1 git pull`. `gh pr list --state open --assignee @me`. CI green on **your** PRs → squash-merge then delete branch. **Do not poll CI.** Do **not** mass-merge Dependabot.
2. Read **`docs/AGENT_EXECUTION_GUIDE.md`** then **`AGENTS.md`**. Guide **wins** on epic order vs this stub.
3. If user says **“lanjut”** without a named slice: **Demo-ok B1–B4 is proven.** Remaining Sentence-ok bars are **human**: C1 three Host `service_id` (off-repo, no SIDs in git) + owner dates §7 E. Short “ganti Imunify…” **forbidden**. **Do not** treat as a code epic. **Do not** re-wipe `tc5` / re-enroll / re-apply-vhost unless the user names it. **Do not** silent-code Guard Discover / bounce / WeasyPrint / WAF 1147+ / G/H / Host invoice.
4. Sentence-ok (only if asked): long pitch only ([`vps-displace-imunify-dev-plan.md`](docs/specs/vps-displace-imunify-dev-plan.md) §0). File-only demo = quote **Host Basic**. WAF/protect sentence = org is **Host Multi before** the meeting. Buyer who **requires** PD / KernelCare / WebShield is **not** a displace deal — sit beside; sell Scan attach.
5. Host WAF live lab (only if asked again): leftover `lab-host-waf-fixture` may already exist (`--keep-site`). `HOST_WAF_LAB_ALLOW_PUBLIC_PROD=1`; `HOST_WAF_LAB_VHOST_SSH=tc5`; `./scripts/host-waf-lab-smoke.sh --apply-vhost` copies to `/tmp/sinexis-host-waf-lab.conf` — **operator must include** on lab vhost (already included). `sudo nginx -t`. Standing permission **2026-08-26** is **Guard enroll/unenroll** (wipe `tc5` first, guide **§4.1**), not a blank cheque to keep applying vhosts. **Never** wipe `sx-erpstg`. Playwright ≠ enroll.
6. Guard enroll (only if asked): wipe-first §4.1. Stdin import is **unreliable**; import with `-i "$KEY"` + confirm `y`. Never delete Manager **000**. Never wipe agent **010** (`sx-erpstg`).
7. Inbox files if asked: `backend/app/api/inbox_routes.py`, `backend/app/models/email_send_log.py` (`user_id`, `job_id`), `frontend/src/pages/Inbox.tsx`, spec [`inbox-delivered-v1.md`](docs/specs/inbox-delivered-v1.md).
8. Print files if asked: `frontend/src/api/scans.ts` (`printFile`), `ScanDetail.tsx` (`export-print-executive`), `Schedules.tsx` (`schedule-print-executive`), spec [`scan-pdf-invoice-print-v1.md`](docs/specs/scan-pdf-invoice-print-v1.md).
9. Guard D10 if asked: `POST /api/guard/agents/{id}/disable`, `guard_agents.disabled_at`. Hard delete Wazuh = ops.
10. Older snapshots (including **2026-09-20 Sentence-ok docs #809**, **2026-09-17 first-scan/D10**, **2026-09-16 print + inbox**, **2026-09-15 Billing empty**, **invoice tests / metering v3**, **SPA chrome**, **#733 / tc5**): [`docs/archive/handoff-session-snapshots-2026-09.md`](docs/archive/handoff-session-snapshots-2026-09.md). Stuck-job notes: [`docs/archive/handoff-scan-pending-2026.md`](docs/archive/handoff-scan-pending-2026.md).
